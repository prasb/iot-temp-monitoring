{
    "routes": [
        {
           "method": "POST",
           "path": "/password",
           "handler": "password.index",
           "config": {
               "policies": ["plugins::users-permissions.isAuthenticated"]
           }
        }
    ]
} 